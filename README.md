# Hayden Austin — portfolio

A dependency-free personal portfolio built for GitHub Pages with semantic HTML, responsive CSS, and a small amount of vanilla JavaScript.

## Local preview

```bash
python3 -m http.server 8080
```

Open `http://localhost:8080`.

The site is published from the root of the `main` branch at <https://xsolumx.github.io/CV_Website/>.
